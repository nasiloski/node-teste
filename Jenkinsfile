pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'echo "123" > teste.log'
                sh 'mkdir /home/teste/'
                sh 'cp -r * /home/teste/'
                
            }
        }
        
    }
}